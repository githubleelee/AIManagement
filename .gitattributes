# 统一行尾为 LF。
# 团队同时在 Windows 编辑器与 WSL 下工作，若不统一会产生「整文件重写」的假 diff。
* text=auto eol=lf

# ---------- 二进制文件（禁止行尾转换） ----------
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.pdf  binary
*.zip  binary
*.docx binary
*.db   binary
*.sqlite binary

# ---------- 锁文件（合并冲突高发，不参与 diff 统计） ----------
package-lock.json -diff linguist-generated=true
pnpm-lock.yaml    -diff linguist-generated=true

# ---------- 文档不计入代码语言统计 ----------
docs/** linguist-documentation
*.md    linguist-documentation

# 保持 shell 脚本可执行位
*.sh text eol=lf
